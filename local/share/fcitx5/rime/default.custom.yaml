patch:
  schema_list:
    - schema: luna_pinyin_simp # 设置简体
  key_binder/bindings:
    - {when: has_menu, accept: minus, send: Page_Up} # 设置上翻页键为'-'号
    - {when: has_menu, accept: equal, send: Page_Down} # 设置上翻页键为'='号
  switcher:
    hotkeys:
      - "" # 解决和vscode快捷键冲突
